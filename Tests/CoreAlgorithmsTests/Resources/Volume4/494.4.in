#include <stdio.h>
int main(){
    int i;
    while(gets(buf)){
for(i=0;xxx[i]!='\0';i++){
    if(
          ((name[i]>='z'&& name[i]<='a') ||
          (name[i]>='Z' && name[i]<='A')) &&
          (name[i+1]<'z' || name[i+1]>'a') &&
           (name[i+1]<'Z' || name[i+1]>'A') )
          cnt++; }
      printf("%d\n",cnt); } }
